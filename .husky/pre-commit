STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACMR | grep -E '\.(js|jsx|ts|tsx|json|jsonc|css)$' || true)
if [ -n "$STAGED_FILES" ]; then
  echo "$STAGED_FILES" | xargs pnpm biome check --write
fi
# pnpx vitest $(git diff --cached --name-only --diff-filter=ACMR | sed 's| |\\ |g') --run
pnpm t
# intentionally leaving out platform build since that doesn't cache as well
pnpm build
git update-index --again

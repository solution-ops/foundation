# Base URL of the running application (local dev or deployed preview)
BASE_URL="http://localhost:8787"

# Database connection for test seed/cleanup scripts
DATABASE_URL="postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/neondb?sslmode=require"

# Test user credentials — created automatically by the global setup script.
# In CI these are generated per run. Locally you can set fixed values.
SEED_USER_EMAIL="e2e-local@example.com"
SEED_USER_PASSWORD="TestPassword123!"
